services:
     - type: web
       name: ai-chat-backend
       runtime: python
       buildCommand: pip install -r requirements.txt
       startCommand: gunicorn manage:app --bind 0.0.0.0:$PORT
       envVars:
         - key: PYTHON_VERSION
           value: 3.11.0
         - key: PORT
           value: 10000